<img
    src="{{ $placeholder }}"
    data-src="{{ $src }}"
    alt="{{ $alt }}"
    width="{{ $width }}"
    height="{{ $height }}"
    loading="{{ $loading }}"
    class="lazy-image {{ $class }}"
    style="background: #222;"
>
